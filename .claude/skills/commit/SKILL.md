# Commit Skill
1. Run `git status` and `git diff --staged` to see changes
2. If nothing staged, stage all relevant changes with `git add`
3. Write a concise, descriptive commit message
4. Commit and push to the current branch
5. Show the resulting git log entry
